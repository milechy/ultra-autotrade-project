/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // In production, serve via reverse proxy (recommended). For local dev, the UI calls the backend directly.
};
module.exports = nextConfig;
