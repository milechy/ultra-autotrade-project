**ガイドの要件:**
- 対象読者: パートナー企業の開発者（プロジェクト内部詳細には触れない）
- staging URL: http://77.42.46.155:3000（フロントエンド）/ http://77.42.46.155:8000（バックエンド API）
- 環境: すべて staging（テストネット）。本番資金には一切アクセスしない
- 記載すべき項目:
  1. **概要**: システム全体の目的と、パートナーが検証すべき範囲（読み取り専用の運用ダッシュボード＋AIシグナルフロー）
  2. **アクセス方法**: ログイン情報の取得先（別途共有予定と明示）、staging 環境の制約
  3. **検証シナリオ**（4件）:
     - シナリオ A: ダッシュボード閲覧（/dashboard/automation で稼働状況確認 → /dashboard/reports で最新レポート確認）
     - シナリオ B: AI シグナルフロー確認（POST /api/ai/analyze で BUY/SELL/HOLD レスポンスを確認 → POST /api/octobot/signal でシグナル送信結果の確認）
     - シナリオ C: Aave リバランス dry-run（POST /api/aave/rebalance で dry_run=true で各アクションの結果確認）
     - シナリオ D: 緊急停止状態の確認（GET /api/automation/status で is_trading_paused の確認）
  4. **制約と注意事項**: 状態変更操作は読み取り専用のダッシュボード経由のみ。直接トランザクション送信は行わない。dry_run以外の Aave 操作は実行しない
  5. **問題報告**: バグや不整合の報告先と、再現手順の記載フォーマット

- ガイド全体のトーン: 丁寧だが簡潔。画面遷移やAPIリクエスト例は具体的に書く
- 既存の docs/19_operations_runbook.md §2.5（運用ダッシュボード UI）と整合させる