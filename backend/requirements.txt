# backend/requirements.txt

slowapi>=0.1.9
fastapi
uvicorn[standard]
httpx
pydantic
pytest
pytest-asyncio>=0.23.0

# Web3 / Aave integration
web3>=6.15.1
eth-account>=0.11.0
python-dotenv>=1.0.0

# Authentication & Database (Phase 12)
sqlalchemy>=2.0.0
bcrypt>=4.1.0
PyJWT[crypto]>=2.9.0    # [crypto] extra で cryptography (RS256/ES256 等) を有効化 — Privy ID Token 検証に必要
pydantic[email]>=2.0.0

# PostgreSQL + pgvector (PoC Pivot)
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
pgvector>=0.2.0
alembic>=1.13,<2.0

# Exchange (PoC Pivot)
ccxt>=4.0.0

# AI/LLM (PoC Pivot)
openai>=2.28.0
anthropic>=0.25.0
tiktoken>=0.6.0

# Knowledge Hub scraping (PoC Pivot)
beautifulsoup4>=4.12.0
feedparser>=6.0.0
lxml>=5.0.0

# Web Push (Phase PWA)
pywebpush>=2.0.0

# Cython (AI logic protection)
Cython>=3.0.0

# Build tools (pinned for CVE-2026-24049)
wheel>=0.46.2
