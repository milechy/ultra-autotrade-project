# Development & Testing dependencies
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12
vcrpy>=6.0
pytest-recording>=0.13.0

# Linting & Type checking
ruff>=0.4.0
mypy>=1.10

# Type stubs
types-requests>=2.31

# ── PoC: TimesFM forecasting (Asana 1214077580922982) ─────────────────────────
# Heavy ML stack (~2GB checkpoint, ~4GB peak RSS). Not installed by default to
# keep verify.sh fast. Run scripts/timesfm_poc.py in a dedicated Python 3.11
# venv. See docs/51_timesfm_poc_report.md §7 for setup.
#
# timesfm[torch]>=1.3.0
# prophet>=1.1
# statsmodels>=0.14
