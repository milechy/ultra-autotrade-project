[pytest]
pythonpath =
    backend

testpaths =
    backend/tests

# pytest-asyncio configuration
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

# Custom markers
markers =
    e2e: End-to-end tests requiring Mumbai testnet connection (RUN_E2E_TESTS=1)
    integration: Integration tests
